# Force npm to use a public mirror (often bypasses corporate blocks)
registry=https://registry.npmmirror.com/

# Relax SSL strictness (fixes issues with Zscaler/Corporate Proxies)
strict-ssl=false

# Increase timeout for slow internal networks
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
