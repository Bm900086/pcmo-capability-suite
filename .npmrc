# Use public mirror registry to bypass corporate firewall
registry=https://registry.npmmirror.com/

# Relax SSL strictness due to corporate proxy interception
strict-ssl=false

# Increase timeouts for slow internal networks
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
